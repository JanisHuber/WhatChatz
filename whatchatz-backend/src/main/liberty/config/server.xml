<?xml version="1.0" encoding="UTF-8"?>
<server description="Simple WebSocket Demo Server">

    <!-- Enable features -->
    <featureManager>
        <feature>jakartaee-10.0</feature>
    </featureManager>

    <!-- HTTP endpoint - only HTTP for simple demo -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  host="*"/>

    <!-- Enable CORS for all origins, methods, and headers -->
    <httpDispatcher>
        <cors enabled="true"
              domain="*"
              allowCredentials="true"
              allowedMethods="GET,POST,PUT,DELETE,OPTIONS"
              allowedHeaders="origin, content-type, accept, authorization"
              maxAge="3600"/>
    </httpDispatcher>

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <!-- Configures the application on a specified context root -->
    <webApplication contextRoot="/websocket-app" location="websocket-app.war"/>
    <!--
    <dataSource id="whatchatz" jndiName="jdbc/whatchatz" includeEnvVars="true">
        <jdbcDriver libraryRef="mariadb" javax.sql.DataSource="org.mariadb.jdbc.MariaDbDataSource"
                    javax.sql.XADataSource="org.mariadb.jdbc.MariaDbDataSource"
                    javax.sql.ConnectionPoolDataSource="org.mariadb.jdbc.MariaDbDataSource"/>

        <properties url="jdbc:mariadb://${DB_HOST}:${DB_PORT}/${DB_NAME}"
                    user="${DB_USER}"
                    password="${DB_PASSWORD}"/>
    </dataSource>
    -->
    <dataSource id="whatchatz" jndiName="jdbc/whatchatz">
        <jdbcDriver libraryRef="mariadb"
                    javax.sql.DataSource="org.mariadb.jdbc.MariaDbDataSource"
                    javax.sql.XADataSource="org.mariadb.jdbc.MariaDbDataSource"
                    javax.sql.ConnectionPoolDataSource="org.mariadb.jdbc.MariaDbDataSource"/>

        <properties
                url="jdbc:mariadb://localhost:3306/whatchatz"
                user="root"
                password="1234"/>
    </dataSource>

    <library id="mariadb">
        <fileset dir="resources" includes="mariadb-java-client-*.jar"/>
    </library>
</server>
